name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  checks:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python: ['3.11', '3.13']
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python }}
      - uses: astral-sh/setup-uv@v10.2.0
        with:
          version: latest-known
      - run: uv sync --locked --group dev
      - run: uv run --locked pytest --cov=typedrank --cov-report=term-missing
      - run: uv run --locked ruff check .
      - run: uv run --locked ruff format --check .
      - run: uv run --locked mypy src/typedrank benchmarks/compare.py
      - run: uv run --locked python -m build
      - run: uv run --locked python benchmarks/compare.py --output benchmarks/ci-smoke.json
